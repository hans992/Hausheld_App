# Backend API base URL (admin uses same backend as mobile app).
# Use /api when using Vite proxy; or full URL e.g. http://localhost:8000
VITE_API_URL=http://localhost:8000

# Mapbox access token for the geo map (Map page). Get one at https://account.mapbox.com/
VITE_MAPBOX_TOKEN=
